version: '3.8'
services:
  stock-sync:
    build: .
    environment:
      MYSQL_USER: root
      MYSQL_PASSWORD: InsightOne123456
      MYSQL_HOST: mysql
      MYSQL_DB: stock_db
    volumes:
      - ./code.csv:/app/code.csv:ro
      - ./logs:/app/logs
    depends_on:
      - mysql
    restart: unless-stopped