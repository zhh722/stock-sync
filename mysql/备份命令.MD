# 1.创建网络(如已创建，忽略此步)
docker network create public_net

# 2. 启动
docker-compose down -v && docker-compose up -d --build

# 导出数据
```text
mysqldump -u root -pInsightOne123456 stock_db > stock_db_$(date +%Y%m%d).sql
```
# 导入数据
```text
mysql -u root -pInsightOne123456 stock_db < backup_file.sql
```